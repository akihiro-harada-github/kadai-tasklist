<% if logged_in? %>
  <p>ログイン完了ユーザー: <%= current_user.name %></p>
    <%= link_to "新規タスクの投稿", login_path %>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>タスク管理</h1>
      <%= link_to "Sign up now!", signup_path, class: "btn btn-lg btn-primary" %>
    </div>
  </div>
<% end %>